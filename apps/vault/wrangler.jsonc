// apps/vault/wrangler.jsonc — Vault.
// scenarios, questions, runs, outcomes — the archive.
//
// GENERATED by `bun run gen:wrangler` from the resource table in
// @seldon/foundation. Edit that table, not this file: `bun run
// infra:check` fails on drift. Ids come from the synced Pulumi
// stack outputs in infra/outputs/.
{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "seldon-vault-dev",
  "main": "src/index.ts",
  "compatibility_date": "2026-08-01",
  "compatibility_flags": ["nodejs_compat"],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1
  },
  "vars": {
    "ENVIRONMENT": "dev",
    "BUILD_VERSION": "dev"
  },
  "d1_databases": [
    {
      "binding": "VAULT_DB",
      "database_name": "seldon-vault-db-dev",
      "database_id": "local",
      "migrations_dir": "migrations"
    }
  ],
  "r2_buckets": [
    {
      "binding": "RUN_BUCKET",
      "bucket_name": "seldon-runs-dev"
    }
  ],
  "services": [
    {
      "binding": "PSYCHOHISTORY",
      "service": "seldon-psychohistory-dev",
      "entrypoint": "PsychohistoryEntrypoint"
    }
  ],
  "env": {
    "staging": {
      "name": "seldon-vault-staging",
      "vars": {
        "ENVIRONMENT": "staging",
        "BUILD_VERSION": "dev"
      },
      "d1_databases": [
        {
          "binding": "VAULT_DB",
          "database_name": "seldon-vault-db-staging",
          "database_id": "pulumi:d1:seldon-vault-db-staging",
          "migrations_dir": "migrations"
        }
      ],
      "r2_buckets": [
        {
          "binding": "RUN_BUCKET",
          "bucket_name": "seldon-runs-staging"
        }
      ],
      "services": [
        {
          "binding": "PSYCHOHISTORY",
          "service": "seldon-psychohistory-staging",
          "entrypoint": "PsychohistoryEntrypoint"
        }
      ]
    },
    "production": {
      "name": "seldon-vault-production",
      "vars": {
        "ENVIRONMENT": "production",
        "BUILD_VERSION": "dev"
      },
      "d1_databases": [
        {
          "binding": "VAULT_DB",
          "database_name": "seldon-vault-db-production",
          "database_id": "pulumi:d1:seldon-vault-db-production",
          "migrations_dir": "migrations"
        }
      ],
      "r2_buckets": [
        {
          "binding": "RUN_BUCKET",
          "bucket_name": "seldon-runs-production"
        }
      ],
      "services": [
        {
          "binding": "PSYCHOHISTORY",
          "service": "seldon-psychohistory-production",
          "entrypoint": "PsychohistoryEntrypoint"
        }
      ]
    }
  }
}

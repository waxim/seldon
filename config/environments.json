{
  "$comment": [
    "Per-environment account facts shared by infra/ (Pulumi) and the",
    "generated wrangler.jsonc route blocks. Hostnames use the reserved",
    ".example TLD until the real zone exists; `bun run infra:check",
    "--strict` refuses to pass while they are still placeholders, so a",
    "deploy cannot quietly ship them."
  ],
  "compatibilityDate": "2026-08-01",
  "compatibilityFlags": ["nodejs_compat"],
  "environments": {
    "staging": {
      "zone": "seldon.example",
      "consoleHostname": "terminus-staging.seldon.example",
      "apiHostname": "api-staging.seldon.example",
      "accessTeamDomain": "seldon.cloudflareaccess.com"
    },
    "production": {
      "zone": "seldon.example",
      "consoleHostname": "terminus.seldon.example",
      "apiHostname": "api.seldon.example",
      "accessTeamDomain": "seldon.cloudflareaccess.com"
    }
  }
}
